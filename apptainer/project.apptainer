Bootstrap: localimage
From: images/postgres:latest.sif

%files
    .. /pandora

%post
    apt update
    apt-get -y install python3 python3-pip git

    cd /pandora

    # base package + RSA optional dependencies from pyproject.toml
    pip3 install --break-system-packages ".[rsa]"